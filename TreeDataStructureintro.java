public class TreeDataStructureintro {
    public static void main(String[] args) {
        //Tree Data Structure - a tree is a non linear data structure where nodes are organised in a heirarchy
        //                    - used in = file explorer
        //                              = database
        //                              = DNA (domain name server)
        //                              = HTML DOM (document object model)

        // Out going edges of tree = Parent
        // In coming edges of tree = Child
        // Two children sharing same parent = Sibling

        // subtree = a smaller tree held within a larger tree
        // size of tree = number of nodes 
        // depth of node = number of edges below the root node -> depth of A = 0 ; depth of B & C = 1 ; depth of D,E,F = 2
        //height of node = number of edges above the furthest leaf node -> height of A = 2 ; height of B & C = 1 ; height of D,E,F = 0
        
    }
}
